
-
  function dummyimage(width, height, color) {
    if (!width) width = random(1000, 700);
    if (!height) height = random(1000, 700);

    color = color || 'ebe8eb';

    return 'https://dummyimage.com/'+width+'x'+height+'/'+color
  }
