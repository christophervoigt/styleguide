
mixin sg-usedModules()
  -
    if (self.usedModules.length>0)
      var usedModule = self.usedModules
    else
      return

  .sg-usedModules
    | Used modules:
    ul
     each module in usedModule
       li=module
